class Nampy:

    def __init__(self,value) -> None:
        pass

    def array(self, v1, v2):
    

        # f string function
        # return [v1[i] + v2[i]  for i in range(len(v1))]

        # zip function

        # for item1, item2 in zip(v1, v2):
        #     return v1 + v2

        # custome logic
        # new_list = []
        # for i in range(len(v1)):
        #     new_list.append(v1[i] + v2[i])
        # return new_list
